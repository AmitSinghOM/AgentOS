global:
  scrape_interval: 5s

scrape_configs:
  - job_name: agentos-api
    metrics_path: /metrics
    static_configs:
      # The API runs on the host in the quick start; inside compose use the service name.
      - targets: ["host.docker.internal:8000"]
